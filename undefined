import PersonaForm from "@/components/persona-form";

export default function PersonaFormStoryboard() {
  // Mock user and profile data for the storyboard
  const mockUser = {
    id: "user-123",
    email: "user@example.com",
    user_metadata: {
      full_name: "John Doe"
    }
  };
  
  const mockProfile = {
    id: "profile-123",
    full_name: "John Doe",
    bio: "I'm a software developer with a passion for building user-friendly applications.",
    passions: "Programming, hiking, photography, and learning new technologies.",
    work_projects: "Currently working on a project tracking application and contributing to open source projects."
  };

  return (
    <div className="bg-background min-h-screen p-8">
      <div className="w-full max-w-3xl mx-auto">
        <h1 className="text-2xl font-bold mb-6">Persona Form</h1>
        <div className="bg-card border rounded-lg shadow-sm p-6">
          <PersonaForm user={mockUser} profile={mockProfile} />
        </div>
      </div>
    </div>
  );
}
